import { CandidateStatusPipe } from './candidate-status.pipe';

describe('CandidateStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new CandidateStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
