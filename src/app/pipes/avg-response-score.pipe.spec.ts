import { AvgResponseScorePipe } from './avg-response-score.pipe';

describe('AvgResponseScorePipe', () => {
  it('create an instance', () => {
    const pipe = new AvgResponseScorePipe();
    expect(pipe).toBeTruthy();
  });
});
